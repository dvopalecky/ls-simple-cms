<!doctype html>
<html lang="en-US">
  <head>
    <title>Simple CMS App</title>
    <meta charset="UTF-8">
  </head>
  <body>
    <% if session[:error] %>
      <div>
        <p><%= session.delete(:error) %></p>
      </div>
    <% end %>
    <main>
      <%= yield %>
    </main>
  </body>
</html>
